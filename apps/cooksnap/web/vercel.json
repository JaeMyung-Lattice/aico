{
  "installCommand": "cd ../../.. && pnpm install --filter=@cooksnap/web...",
  "buildCommand": "cd ../../.. && pnpm turbo run build --filter=@cooksnap/web",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
