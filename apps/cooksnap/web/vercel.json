{
  "installCommand": "rm -rf node_modules && cd ../../.. && rm -rf node_modules && pnpm install",
  "buildCommand": "npx vite build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
